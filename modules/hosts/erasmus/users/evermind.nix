# Host-specific binding for evermind on erasmus (NixOS desktop)
# This is where erasmus-specific HM module selections go.
{
  ...
}:
{
  # Currently all evermind config is in modules/users/evermind/
  # If erasmus needed different HM modules than another host,
  # override here.
}
